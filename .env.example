# Application
PORT=3000
PING_MESSAGE=pong

# SQLite (default when running the container)
# The app will create/use /app/data/app.db inside the container.
LOCAL_DB_DIR=/app/data
# Alternatively, set an explicit file path:
# LOCAL_DB_PATH=/app/data/app.db

# MySQL (optional)
# Uncomment and set these to use MySQL instead of SQLite
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_DATABASE=app
MYSQL_USER=app
MYSQL_PASSWORD=apppw

# MySQL root (compose-only convenience)
MYSQL_ROOT_PASSWORD=rootpw